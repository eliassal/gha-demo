name: Env Demo
on: workflow_dispatch
env:
    APP_ENV: "dev"
jobs:
    env_dmo:
        runs-on: ubuntu-latest
        steps:
            - name: Env Demo Step
              run: echo "Hello, $APP_ENV"
